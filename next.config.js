
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost']
  }
}

// module.exports = withNextIntl({
//   images: {
//     domains: ['picsum.photos', 'localhost']
//   }
// });
